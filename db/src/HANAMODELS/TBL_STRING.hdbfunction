FUNCTION "XSA1.db.HANAMODELS::TBL_STRING"()
  RETURNS TABLE (
    CUSTID NVARCHAR(10),
    PRODID NVARCHAR(10),
    QTY INTEGER,
    REV INTEGER
  )
  LANGUAGE SQLSCRIPT
  SQL SECURITY INVOKER AS
BEGIN
  RETURN 
  SELECT CUSTID, PRODID, QTY, REV FROM "XSA1.db.SYNONYMS::SALES";
END;
